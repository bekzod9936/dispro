import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        fontSize: 18,
        fontWeight: 500,
        color: '#A5A5A5',
        marginRight: 20,
        lineHeight: '21.09px'
    }
})